name: Fetch Store JSON form GAS

on:
  schedule:
    # Daily at 8:20 UTC
    - cron: "*/15 * * * *"
  push:
    branches:
      - main
    paths:
      - "dist/**"
  workflow_dispatch:

jobs:
  fetch-json:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@master
      - name: Setup python3
        uses: actions/setup-python@v3
        with:
          python-version: "3"
      - name: Install Python packages
        run: |
          pip install --upgrade pip
          pip install requests requests_html
      - name: Run Script
        run: python src/py-crawler/fetch-store.py > dist/store.json
      - name: Commit files
        run: |
          git config user.email "hi+git@imych.one"
          git config user.name "Github Action"
          git add -A
          git commit -m "Fetch latest JSON from GAS API" -a
          git push
